# Security

Even without authentication in this delivery, the foundation enforces:

- security headers
- restricted CORS
- request validation
- body size limits
- no stack traces in responses
- no credentials in logs

Initial allowed frontend origin:

```text
http://localhost:5173
```
